# Java Spring Security 规范

> 状态：**占位**。暂不形成强制条文。  
> 现有约定仍落在：`Java-代码架构.md`（`security/` 包放哪）、`Java-模块代码规范.md`（security 职责边界）、`Java-Spring规范.md`（Filter 不写业务用例）。

---

（预留。若后续有可复用的通用写法再补；项目定制的鉴权方案不必硬收成 skill。）
